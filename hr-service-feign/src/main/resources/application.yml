spring:
 application:
  name: hr-service-feign
  
server:
 port: 8099

eureka:
 client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
     defaultZone: http://localhost:8761/eureka/
     
logging:
  level:
    '[org.springframework.cloud]': debug